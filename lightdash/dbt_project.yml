name: lightdash_project
version: 1.0.0
config-version: 2

profile: lightdash_project

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  lightdash_project:
    +materialized: view
    financeiro:
      +schema: financeiro
      +materialized: view
